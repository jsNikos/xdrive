var assert = require('assert');
var app = require('../app');
var customerRequestService = require('../services').customerRequestService;

describe('CustomerRequestService', function() {
  describe('#add()', function () {
    it('should add a customerRequest', function (done) {
      done();
    });
  });

});
